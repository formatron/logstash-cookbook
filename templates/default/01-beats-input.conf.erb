input {
  beats {
    port => 5043
    type => "logs"
    <% unless @certificate.nil? %>
    ssl => true
    ssl_certificate => "<%= @certificate %>"
    ssl_key => "<%= @private_key %>"
    <% end %>
  }
}
